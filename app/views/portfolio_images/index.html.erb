<ul id="portfolio-images">
  <% for image in @portfolio_images do %>
    <li><%= link_to(image_tag(image.screenshot.url(:thumb)), image.screenshot.url(:original)) %></li>
    <div><%= image.description %></div>
  <% end -%>
</ul>
